name: GitHub Metrics & Profile Updates

on:
  # Schedule automatic updates
  schedule:
    - cron: "0 0 * * *" # Run daily at midnight UTC
  
  # Allow manual workflow dispatch
  workflow_dispatch:
  
  # Update on push to main branch
  push: 
    branches: 
      - main

jobs:
  github-metrics:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    steps:
      # Checkout repository
      - name:  Checkout repository
        uses: actions/checkout@v4
      
      # Generate base metrics
      - name: Base Metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: asset/base.svg
          user: KunjShah95
          template: classic
          base:  header, activity, community, repositories, metadata
          config_timezone: Asia/Kolkata
          plugin_lines: yes
          plugin_followup: yes
          plugin_followup_sections: repositories
      
      # Generate activity metrics
      - name: Activity Metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: asset/activity.svg
          user: KunjShah95
          template:  classic
          base: ""
          config_timezone: Asia/Kolkata
          plugin_activity: yes
          plugin_activity_days: 30
          plugin_activity_filter: all
          plugin_activity_limit:  5
          plugin_activity_load: 300
          plugin_activity_visibility: all
      
      # Generate language metrics
      - name: Language Metrics
        uses: lowlighter/metrics@latest
        with: 
          token: ${{ secrets. METRICS_TOKEN }}
          filename:  asset/languages.svg
          user: KunjShah95
          template: classic
          base: ""
          config_timezone: Asia/Kolkata
          plugin_languages: yes
          plugin_languages_analysis_timeout: 60
          plugin_languages_categories: markup, programming, data
          plugin_languages_colors: github
          plugin_languages_details: percentage, bytes-size
          plugin_languages_limit: 8
          plugin_languages_recent_categories: markup, programming, data
          plugin_languages_recent_days: 30
          plugin_languages_recent_load: 300
          plugin_languages_sections: most-used
          plugin_languages_threshold: 0%
      
      # Generate calendar metrics
      - name: Calendar Metrics (Isocalendar)
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: asset/isocalendar.svg
          user: KunjShah95
          template: classic
          base: ""
          config_timezone: Asia/Kolkata
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year
      
      # Generate habits metrics
      - name: Habits Metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: asset/habits.svg
          user: KunjShah95
          template: classic
          base: ""
          config_timezone: Asia/Kolkata
          plugin_habits:  yes
          plugin_habits_charts: yes
          plugin_habits_days: 30
          plugin_habits_facts: yes
          plugin_habits_from:  200
      
      # Generate achievements
      - name: Achievements Metrics
        uses: lowlighter/metrics@latest
        with: 
          token: ${{ secrets. METRICS_TOKEN }}
          filename:  asset/achievements.svg
          user: KunjShah95
          template: classic
          base: ""
          config_timezone: Asia/Kolkata
          plugin_achievements: yes
          plugin_achievements_display: detailed
          plugin_achievements_secrets: yes
          plugin_achievements_threshold: C
      
      # Generate notable contributions
      - name: Notable Contributions
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: asset/notable.svg
          user: KunjShah95
          template: classic
          base: ""
          config_timezone: Asia/Kolkata
          plugin_notable:  yes
          plugin_notable_from: organization
          plugin_notable_repositories: yes
      
      # Generate projects metrics
      - name: Projects Metrics
        uses: lowlighter/metrics@latest
        with: 
          token: ${{ secrets. METRICS_TOKEN }}
          filename:  asset/projects.svg
          user: KunjShah95
          template: classic
          base: ""
          config_timezone: Asia/Kolkata
          plugin_projects: yes
          plugin_projects_limit: 4
      
      # Generate stargazers metrics
      - name: Stargazers Metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: asset/stargazers.svg
          user: KunjShah95
          template: classic
          base: ""
          config_timezone: Asia/Kolkata
          plugin_stargazers:  yes
          plugin_stargazers_charts: yes
          plugin_stargazers_charts_type: chartist
      
      # Generate reactions metrics
      - name: Reactions Metrics
        uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          filename: asset/reactions.svg
          user: KunjShah95
          template: classic
          base: ""
          config_timezone: Asia/Kolkata
          plugin_reactions:  yes
          plugin_reactions_display: absolute
          plugin_reactions_limit: 200
          plugin_reactions_limit_discussions: 100
          plugin_reactions_limit_discussions_comments: 100
          plugin_reactions_limit_issues: 100
      
      # Generate contribution snake animation
      - name: Generate Snake Animation
        uses: Platane/snk@v3
        with:
          github_user_name: KunjShah95
          outputs: |
            dist/github-contribution-grid-snake-dark.svg? palette=github-dark
            dist/github-contribution-grid-snake.svg?palette=github-light
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      # Move snake animation to output folder
      - name: Move Snake Animation
        run: |
          mkdir -p output
          mv dist/*. svg output/
      
      # Commit and push changes
      - name: Commit and Push Metrics
        run: |
          git config --local user.email "github-actions[bot]@users. noreply.github.com"
          git config --local user. name "github-actions[bot]"
          git add asset/*. svg output/*. svg
          git diff --quiet && git diff --staged --quiet || (git commit -m "📊 Update metrics - $(date +'%Y-%m-%d %H:%M:%S')" && git push)